# Models Directory

This folder stores trained model weights:

- `default.npz` - Default pre-trained model
- `checkpoints/` - Training checkpoints

## File Format

Models are saved in NumPy's `.npz` format containing:

- Layer weights
- Layer biases
- Network configuration
- Training metadata
