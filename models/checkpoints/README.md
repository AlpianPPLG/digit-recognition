# Checkpoints Directory

Training checkpoints are saved here during model training.
Each checkpoint contains:

- Model weights at that epoch
- Training metrics (loss, accuracy)
- Optimizer state

Naming convention: `checkpoint_epoch_{N}.npz`
